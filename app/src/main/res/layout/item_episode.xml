<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="episode"
            type="com.ssong_develop.rickmorty.entities.Episode" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <include
            android:id="@+id/iv_episode_default"
            layout="@layout/no_such_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            style="@style/BlackColorTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:ellipsize="marquee"
            android:maxLines="2"
            android:text="@{episode.name}"
            android:textSize="24sp"
            app:layout_constraintEnd_toEndOf="@id/iv_episode_default"
            app:layout_constraintStart_toStartOf="@id/iv_episode_default"
            app:layout_constraintTop_toBottomOf="@id/iv_episode_default"
            tools:text="Episode" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>